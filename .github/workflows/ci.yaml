name: ci check
on:
  push:
    branches:
      - main
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        run: echo "github actions triggered"
      - name: step 2
        run: echo "step 2 triggered "
